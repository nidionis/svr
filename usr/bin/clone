#!/bin/bash
clone () 
{ 
	URL=git@github.com:
	PROFIL_NAME=nidionis
	DEFAULT_ADDR=""
    if [ $# -ne "1" ]; then
        PROFIL_NAME="$1";
        PROJECT_NAME="$2";
    else
        PROJECT_NAME="$1";
    fi
	git clone --recurse-submodules $URL$PROFIL_NAME/$PROJECT_NAME.git;
}
clone "$@"

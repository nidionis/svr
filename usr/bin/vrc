#!/bin/bash
vrc () 
{ 
    SOURCE="/home/.bashrc";
    F_NAME=".vimrc";
    FILE=/home/$F_NAME
    F_PATH="$(dirname $FILE)";
    cd "$F_PATH";
    commit_if_modified "$F_NAME";
	if [ "$?" == "0" ] ; then
		cp $F_NAME $MACHINE_DIR/usr/home/.vimrc
	fi
    cd -;
    source $SOURCE
}
vrc "$@"
